# 学习进度追踪

> 状态图例:⬜ 未开始 · 🔄 进行中 · ✅ 已讲解+检验通过 · 📖 已轻讲(不出检验题,仅建立直观,只用于 🟡/⚪)
> 优先级:🔴 P0 核心必学 · 🟡 P1 重要补充 · ⚪ P2 了解/泛读

**当前进度**:一 / **10-spmc 共享内存无锁队列应用(#10,🔴)轮 1 检验已批(见附一)、轮 2 已讲并落笔记、检验待回**([10 笔记](01-低延迟系统开发基础/10-spmc共享内存无锁队列应用.md));上一项 **09-lock-free queue 及 micro-batching(§9,🔴)三轮全部讲完检验完、博客 #4 已上线**([09 笔记](01-低延迟系统开发基础/09-lock-free-queue及micro-batching.md)),轮 3(Logger + MicroBatchProcessor)含改进版 `log()`,检验已批(见附三:Q1 方向对、补了一个接口前提;Q2 对;Q3 问了没讲过的点,已补讲,讲 #10 时接回);09 笔记另有〔补〕(进度保证词汇 + SPSC 是否 wait-free 的外部材料核对,2026-09-20);此前 **07-大页内存、08-内联及内联汇编(🟡,轻讲版)已讲完归档**([07 笔记](01-低延迟系统开发基础/07-大页内存.md) · [08 笔记](01-低延迟系统开发基础/08-内联及内联汇编.md))。

**2026-09-15 流程纠偏**:§5 讲完后误把"讲完这一章最难的一节"当成"讲完阶段一(一+二)",直接跳去了三(socket)——用户当场发现并叫停(**尚未产生实际损失**,socket 笔记没落盘、进度表没改错)。已拉回,并确立新规则:🔴 项维持"讲透+检验"完整轮次;🟡/⚪ 项若判断值得掌握,直接轻讲一遍建立直观、**不出检验题**(用户原话:"帮我建立一个直观就好"),章内仍按编号顺序走,不因优先级重排。规则写进 [README 学习形式表](README.md) 和 tutoring 记忆。

**下一步**:一 / **#9 lock-free queue(🔴)三轮全部完成、博客 #4 已上线** → **#10 spmc队列(🔴):轮 1 检验已批、轮 2 已讲、检验待回(4 题)** → **补课:MESI 缓存一致性状态机(🔴,见一表 5补 行)** → #11 内存对齐(🔴)→ #12 分支预测(🔴)→ 中间 🟡/⚪ 轻讲或跳过 → #24 自旋锁(🔴)→ #30 wait-free(🔴)→ #31(🟡)→ #32 延迟测量(🔴)→ #33(参考资料)。一 讲完后过 二(5 项,3 个 🔴:roofline/TMA/交易系统性能衡量核心要点)。**阶段一(一+二)全部完成后才进三(socket)+网络占位 → 五 → 四 → 七 → 分布式占位 → 六。**

§5 五轮小结:轮1 小本子/store buffer;轮2 收件箱+四种乱序+x86禁三放一+编译器;轮3 release/acquire配对+六枚举+最弱够用;轮4 缓存行/伪共享ping-pong/alignas/Intel CAT(含与轮2边界三轮追问,已拧清"正确性轴(1-3)vs 性能轴(4-5)"框架,写进笔记);轮5 依赖链排布(含推广到 N 循环的多累加器展开+latency×throughput 公式)+三优先+false dependency+register spill+STLF接回轮1。全部检验已回,细节骨架见 05 笔记各轮 附一。

**回炉点堆栈(面试前过一遍)**:§5 —— 轮1 Q1(小本子机制三句复述)、轮2 Q2(编译器 hoist 三件套)、轮3 Q1/Q2/Q3(成对原因/机器码相同前提/happens-before链)、轮4 Q1/Q2(独占权来回抢/L1L2私有vsL3共享)、轮5 Q1(关键路径只数被等待的那条,写法A是1乘+4加不是4乘+4加)/Q3(STLF转发只认单条记录不做拼接);§3 —— find→bind→verify 命令序列 + managed IRQ 的 `-EIO`/`isolcpus=managed_irq`;§2 —— 检验 Q3/Q4 偏弱,且尚无强化笔记文件(只在对话里讲过);§6 —— 轮1 Q1(本质原因收口:各机制共性是"耗时变量、状态依赖",不是机制本身慢)、Q2("永远 O(1)"的原因是 free list 每次 alloc/dealloc 顺手维护、不是 alloc 时现找,不能只重复结论)、Q3(intrusive 链表 ≠ 数组下标实现的链表,真正 intrusive 是 next 字段长在 T 内部);轮2 Q1(`munmap` 不是解锁是把整个映射连根拔起、锁跟着映射一起没了这一步因果要点破;`M_TRIM_THRESHOLD` 管主 Arena 的 `sbrk` 收缩,别和 `M_ARENA_MAX` 管的"非主 Arena mmap 来源"搞混);轮3 Q2(malloc 不确定性 vs 缺页中断——具体机制不同子系统,但"不确定操作提前挪到启动阶段一次做完"是同一模式,别只停在机制层面对比);轮3 的坑本身(`pinned_arena::MemoryBlock::data()` 用 `this+1` 算地址,跟 `blocks[]`/`data_start` 的内存布局假设对不上,真实会写坏下一块的 header——当成一个可复用的"代码 review 找坑"案例记住,不只是记结论);轮4 Q2(析构≠deallocate——`construct`/`destroy` 正常发生,空操作的只是"还内存"这一步,`monotonic_buffer_resource` 是否真有 `reset`/`release` 及其确切语义未核实,别当成确定结论用);轮4 Q3(悬空的是"分配器内部持有、指向 memory_resource 的指针",不是"指向分配器的指针",方向别说反;`resource` 是有名字的局部对象,不是 C++ 严格意义的"临时对象");§9 —— 轮1 Q1(2 的幂 ↔ mask 全 1 ↔ `&` 等价于 `%` 这步机制没落地,别只停在"避免取模"的结论);Q2(STLF 是"同线程读自己刚写的东西为什么快"的硬件实现细节,不是"relaxed 为什么正确"的原因——后者是内存模型的单线程程序序保证,两层别混;acquire 侧"先清空 invalidate queue"的综合是对的,保持);轮2 Q1(缓存为什么恰好该在追上 `head_cached_` 那一刻重新读,不早不晚——消费者真实进度单调不减,缓存只会偏旧这步单向性论证没展开,别只讲两个分支的结论);轮 3 Q1(补的前提:泛型 `MicroBatchProcessor` 对 `QueueType` 只要求 `enqueue`/`dequeue`、看不到 head/tail,想省独立计数器得先把 O(1) 近似 `size()` 写进接口要求;`size()` 先读 tail 后读 head 对任何线程都不出负数,先 head 后 tail 只有第三方线程调用才会出负数)/Q3(推指针的判据是"被指的东西活得比读取者久且不变",不是"是不是字面量";`std::string`/`c_str()`/栈缓冲要在生产者线程拷字节进队列,源码 :349-357 如此,超 255 字符静默截断;讲 #10 时接回"共享内存元素为什么不能放指针");#10 —— 轮 1 Q1("读者增加不影响生产者速度"过强:不阻塞 ≠ 不变慢,实测生产者每条 2.8→28 ns(512 KiB 环、0→5 个忙轮询读者);"读者的只读副本让生产者的写变贵"这个机制留给 MESI 补课接回)。

**博客发布**:
- §1 ✅ **已上线** → https://jasper-aa64.github.io/posts/cpu-affinity-core-isolation-numa/
  (英文,Systems 分类,`homepage: false` 已验证不上首页、进 /categories/systems/;日期 2026-09-09)。
- §5 ✅ **已上线** → https://jasper-aa64.github.io/posts/memory-ordering-false-sharing-dependency-chains/
  (英文,Systems 分类,`homepage: false`,五轮全覆盖;配图两处:store-buffer/invalidate-queue 机制 SVG 一张
  + "One Cache Line, Two Questions" 石墨铅笔插画一张,日期 2026-09-15)。**博客节奏已定:大章(🔴)讲完就发,§2/§3 不补。**
- **2026-09-15 顺带修的基建问题**:发现 GitHub Pages 的 Source 一直是 legacy(`main:/docs`),`docs/` 最后一次真正提交在
  `fb07941`——**早于两篇文章的提交**,导致 9 号那篇和这篇新的在线上全部 404(首页、`/posts/` 列表页能打开,具体文章页、
  `/categories/systems/` 都不行),CI"success"只代表 build 没错,不代表真的在被 serve。已改 Pages Source 为
  GitHub Actions,`docs/` 从 git 移除并加进 `.gitignore`(以后不用再手动同步这个过期文件夹)。改完两篇文章
  和分类页都验证过(HTTP 200 + DOM 里图片/SVG 都正常渲染)。
配图两处:§3 内嵌英文拓扑 SVG;§1 概念插图迁移代价(石墨铅笔风,GPT 生成,已入库
`static/images/cpu-affinity/migration-cost.png`)。图片风格规范:[`docs/illustration-style.md`](docs/illustration-style.md)。
commit `89bf4bd`(含 `layouts/index.html` 的 homepage 过滤),已 push main,CI 部署成功。
注:GitHub 仓库已改名 `jasper-aa64` → `Jasper-aa64`,push 走的重定向,remote URL 可更新。
- §9 ✅ **已上线** → https://jasper-aa64.github.io/posts/lock-free-queue-logger-micro-batching/ (commit `a443bd2`,2026-09-20;英文,Systems,`homepage: false`;源文件 [lock-free-queue-logger-micro-batching.md](../../../../jasper-aa64.github.io/content/posts/lock-free-queue-logger-micro-batching.md))。内嵌 SVG 一张(一条日志 30 个槽 vs 5 个槽)+ 第 4 节石墨铅笔插画 "Mirror the Addresses, Not the Data"(用户用 GPT 生成,`static/images/lock-free-queue/mirror-the-addresses.png`,2.0 MB)。发布前用新装的 Hugo 0.160.1(与 CI 同版本)整站构建,本地起服务在真实浏览器里查过 DOM;文中 C++ 代码块已用 g++ 16.2.0(`-std=c++20 -Wall -Wextra -Wpedantic`)编译并跑通。上线后 live DOM 核对:SVG 39 rect / 9 text / 0 个 stray `<p>`,配图 1672×941 加载成功,不在首页、在 /posts/ /categories/systems/ /archives/ 里。 **后续(2026-09-21/22,均已在 origin/main)**:插图 2 原图映射画错(相邻两块并成一块、两次映射指向不同物理页)→ 已换成三行对齐、同号垂直虚线版(commit `d8a0dff`,同一提交还带上了 allocator 文章的改动);`7eed74a`(09-22)把 PNG 转成 JPG(`static/images/lock-free-queue/mirror-the-addresses.jpg`,117 KB,线上哈希与仓库一致,上文的 .png/2.0 MB 已过期);残留小瑕疵:虚线弯钩箭头落在第二行右端、而不是 0–1 处的箭头起点,不影响映射。

---

## 学习顺序

| 阶段 | 内容 | 目标窗口 |
|---|---|---|
| 1 | 一(低延迟基础)+ 二(性能方法论) | 现在 → 10 月 |
| 2 | 三(socket)+ 网络与内核旁路占位 | |
| 3 | 五(系统设计) | |
| 4 | 四(交易数据处理) | |
| 5 | 七(数字货币) | |
| 6 | 分布式占位(Raft) | |
| 7 | 六(计算层,泛读) | |

---

## 一、低延迟系统开发基础

| # | 主题 | 优先级 | 状态 | 强化笔记 |
|---|---|---|---|---|
| 1 | CPU亲和性及NUMA架构 | 🔴 | ✅ | [01](01-低延迟系统开发基础/01-CPU亲和性与NUMA架构.md) — 轮1 绑核/隔离 ✅ · 轮2 超线程/锁频 ✅ · 轮3 NUMA ✅ · 叙述版已合一 + 拓扑图 + 英文博客(cpu-affinity-core-isolation-numa)+ 迁移代价插图提示词 ✅ |
| 2 | 实时线程优先级 | 🔴 | ✅ | 轮1 调度策略/实时优先级 ✅ · 轮2 优先级反转/PI/PP/PREEMPT_RT ✅ · 检验 Q3/Q4 偏弱待回炉 |
| 3 | 中断绑定及常见中断核心隔离 | 🟡 | ✅ | [03](01-低延迟系统开发基础/03-中断绑定与核心隔离.md) — 单轮:上/下半部 + ksoftirqd 归属 + 找绑验三步 + 三坑(managed IRQ/irqbalance/isolcpus)+ NUMA 多队列;检验 Q1 满分 · Q2/Q3 有回炉点(命令序列 / managed IRQ) |
| 4 | 系统静默配置步骤总结 | 🟡 | 📖 | [04](01-低延迟系统开发基础/04-系统静默配置步骤总结.md) — 轻讲版(不出检验题):四类启动参数归类 + process_helper 代码(§1+§2+§3 的生产化封装) |
| 5 | 内存模型与缓存及流水线 | 🔴 | ✅ | [05](01-低延迟系统开发基础/05-内存模型与缓存及流水线.md) — 全 5 轮讲完,检验均已回。轮1 store buffer"小本子"(Q1 回炉)· 轮2 收件箱+四种乱序+x86禁三放一+编译器(Q2 轻回炉)· 轮3 release/acquire 配对+六枚举+最弱够用+volatile(Q1 回炉/Q2/Q3 轻回炉)· 轮4 缓存行搬运单位+伪共享ping-pong+alignas+干跑反用+Intel CAT(Q1 回炉/Q2 讲清/Q3 对;轮2/4边界追问三轮已拧清正确性轴vs性能轴)· 轮5 依赖链排布+三优先+false dependency+register spill+STLF接回轮1(Q1/Q3 轻回炉)。§5 是全篇最大最难的一章,回炉点面试前务必过一遍。 |
| 5补 | MESI 缓存一致性状态机(补课) | 🔴 | ⬜ | **2026-09-24 用户提出**(#5 一直"点名但没讲")。现状:#5 轮 2/轮 4 已讲机制(失效通知、写要独占、S↔M 来回、HITM),但状态机本身没单独讲,[05 精读:450](01-低延迟系统开发基础/05-内存模型与缓存及流水线.md:450) 那行写的是"不用背"。**排法:#10 收尾后、#11 之前讲一轮**;笔记建 `01-低延迟系统开发基础/05-补-MESI缓存一致性状态机.md`(排在 05 后面),建好后在 #5 :450、#9 :27 加回指。源笔记没有专门讲解,只有引用:[05:35](../trading-system-notes/chinese/01-low-latency/05-内存模型与缓存及流水线.md:35)(失效队列处提到)、[05:199-203](../trading-system-notes/chinese/01-low-latency/05-内存模型与缓存及流水线.md:199)(两条参考链接:perf-book 13.7 + MESI 动画演示)、[09:8](../trading-system-notes/chinese/01-low-latency/09-lock-free-queue及micro-batching.md:8)(无锁三件套)、[24:387](../trading-system-notes/chinese/01-low-latency/24-自定义自旋锁实现.md:387)(地址监控靠一致性硬件唤醒)。要点:4 态 + 本核/他核读写事件表;读未命中才可能得 E(无人持有),写未命中直接 I→M(RFO);M 被他核读 → 转发并降 S(HITM),被他核写 → M→I;**MESI 保证的是每条缓存行的各份副本一致,不保证多行/多字读取不撕裂、也不规定跨地址的顺序**(撕裂读:#10 实测源码队列约 5.5% 被接受的读取撕裂;顺序:接 #5 小本子/收件箱);现实是 MESIF(Intel)/MOESI(AMD)+ snoop filter/目录,不是一根真总线。应用:计数器 ping-pong、SPSC 缓存游标(#9)、SPMC 读者各持 S 副本 → 写者每圈升级、代价随读者数涨(#10)、伪共享。不默认跑实验(用户 2026-09-24:在学习,没要求在本机跑,费时间),用 #10 附一已有的数;补课里要讲清:代价 = 取数据的那一层 + 是否跨核确认,I→M 与 S→M 没有固定高低(用户追问过,见 #10 附一〔补〕);写未命中直接 I→M,E 只在读未命中且无人持有时停留。素材:用户贴的外部"5 场景"总结**不原样收录**,核出 5 处问题:①"读共享(S)防撕裂"错;②把 ring 里生产者写槽位说成 E→M 静默写(写未命中是 I→M,E 只来自无人持有的读未命中);③"淘汰自还原"当设计保证(小 ring 根本不被淘汰,靠的是失效);④"流水线挂起"忽略 store buffer(普通 store 不停,`lock` 前缀 RMW/fence 才等);⑤缺 M→I(伪共享乒乓的核心一步)。①按 #10 实测,②–⑤按协议定义推;"共享后写变贵"已有 #10 附一的数字(独占连写 2.8 ns/条 → 一个读者轮询后 18.5 ns/条),"对方持 M 时写"没测。 |
| 6 | 内存池 | 🔴 | ✅ | [06](01-低延迟系统开发基础/06-内存池.md) — 四轮全部讲完检验完。轮1 对象池两版;轮2 内存调优+TLB shootdown;轮3 pinned_arena+STL 分配器(含一个〔补〕的真实 bug 及修法);轮4 `std::pmr`(Q2 轻回炉析构≠deallocate,Q3 轻回炉悬空指针方向+"临时对象"术语)。细节见笔记附一~附四 |
| 7 | 大页内存 | 🟡 | 📖 | [07](01-低延迟系统开发基础/07-大页内存.md) — 轻讲版:TLB/页表层级原理 + 四种大页获取方式(mmap 显式大页是 HFT 实际用的,THP 要禁用)+ `HugePageAllocator` 参考实现(纯静态工具类,只换池子的"存储来源") |
| 8 | 内联及内联汇编 | 🟡 | 📖 | [08](01-低延迟系统开发基础/08-内联及内联汇编.md) — 轻讲版:`inline` 链接期(ODR)vs 编译器优化两种含义、`always_inline`/`flatten`、i-cache 膨胀坑、内联汇编 GNU 风格(输出/输入/clobber)+ VEX 混用 penalty、手写汇编技巧表(接回 §5 轮5 popcnt false dependency) |
| 9 | lock-free queue 及 micro-batching | 🔴 | ✅ | [09](01-低延迟系统开发基础/09-lock-free-queue及micro-batching.md) — 轮 1 讲完检验完:`LFQueue<T>` SPSC 环形队列(2 的幂容量+mask/两步生产消费接口/memory_order 接回 §5)+〔补〕运算符优先级真实 bug + `num_elements_` 独占权代价讨论。轮 2 讲完检验完:本地缓存对方游标(接回 §5 影子副本)+ double-mmap 跨尾写入优化(接回 #7 TLB)。轮 3(Logger+MicroBatchProcessor)讲完检验完(含改进版 `log()`:段压入,示例 30 次 push → 5 次;Q1 方向对+补接口前提、Q2 对、Q3 问了没讲过的点已补讲、#10 时接回,见附三)。另有〔补〕:进度保证词汇 + SPSC 是否 wait-free(2026-09-20 外部材料核对) |
| 10 | spmc 共享内存无锁队列应用 | 🔴 | 🔄 | [10](01-低延迟系统开发基础/10-spmc共享内存无锁队列应用.md) — 轮 1 ✅ 检验已批(Q1 轻回炉:"读者增加不影响生产者速度"过强,实测 2.8→28 ns/条;Q2 对,补差值必为 CNT 的整数倍、源码不把差值交出来;Q3 对,追问放进轮 2)。轮 2 已讲、检验待回(4 题):shmmap 四步 + 靠零初始化当空队列、两个进程里的映射地址不同(实测;同 exe 的字面量地址碰巧相同;libstdc++ 短 string 的 data 指针指向自己)→ 元素必须自包含(接回 09 附三 Q3,并更正了 09 里"字面量地址通常不能用"的说法);三个坑:第一圈缺页(56 vs 9 ns/条,每页约 1.5 µs)/ 慢读者被套圈 / 重启续读与队列重建后老读者卡死。 |
| 11 | 内存对齐与典型内存布局优化 | 🔴 | ⬜ | |
| 12 | 分支优化与分支预测 | 🔴 | ⬜ | |
| 13 | 组合优先于继承 | ⚪ | ⬜ | |
| 14 | 编译期多态及编译期计算 | 🟡 | ⬜ | |
| 15 | 循环优化 | 🟡 | ⬜ | |
| 16 | 指针访存优化 | 🟡 | ⬜ | |
| 17 | 关于函数的潜在优化 | ⚪ | ⬜ | |
| 18 | 强度削弱优化及算术优化 | ⚪ | ⬜ | |
| 19 | 直接操作内存二进制表示 | 🟡 | ⬜ | |
| 20 | 无法内联的函数调用优化 | ⚪ | ⬜ | |
| 21 | 缓存预取预热和向量化 | 🟡 | ⬜ | |
| 22 | HPC 辅助宏 | ⚪ | ⬜ | |
| 23 | 双数组加原子索引实现数据更新和访问 | 🟡 | ⬜ | |
| 24 | 自定义自旋锁实现 | 🔴 | ⬜ | |
| 25 | 位域与位运算 | 🟡 | ⬜ | |
| 26 | C++20 协程调度框架 | 🟡 | ⬜ | |
| 27 | 常用设计模式 | ⚪ | ⬜ | |
| 28 | C++ 函数传参问题及优化 | ⚪ | ⬜ | |
| 29 | C++ 边界检查优化技术 | ⚪ | ⬜ | |
| 30 | wait-free 编程 | 🔴 | ⬜ | [30](01-低延迟系统开发基础/30-wait-free编程.md) — **预读稿(2026-09-20,未讲、无检验)**:sticky counter 的 lock-free 对照版、wait-free 版逐步走查("窗口"/helping)、`exchange` 是"认领权只发一张"(纠正知乎文章的解释)、Python 穷举验证([脚本](01-低延迟系统开发基础/30-sticky-counter-model-check.py):3 个变体各有反例;`read()` 的非零值只是近似)、取舍与 ARM 无 LSE 时的硬件依赖。四档梯度 + SPSC 是否 wait-free 在 [09 笔记〔补〕](01-低延迟系统开发基础/09-lock-free-queue及micro-batching.md)。讲到这里时仍走完整一轮 + 检验。 |
| 31 | Linux 内核调优和 BIOS 配置 | 🟡 | ⬜ | |
| 32 | 延迟测量(时钟周期) | 🔴 | ⬜ | |
| 33 | 系统设计优质文章 | ⚪ | ⬜ | 参考资料,穿插看 |

## 二、常见性能瓶颈与优化方向

| # | 主题 | 优先级 | 状态 | 强化笔记 |
|---|---|---|---|---|
| 1 | roofline model | 🔴 | ⬜ | |
| 2 | TMA 方法 | 🔴 | ⬜ | |
| 3 | 利用编译器等优化程序 | 🟡 | ⬜ | |
| 4 | 特殊硬件指令(AVX/SIMD) | 🟡 | ⬜ | |
| 5 | 交易系统性能衡量的核心要点 | 🔴 | ⬜ | |

## 三、简单实践

| # | 主题 | 优先级 | 状态 | 强化笔记 |
|---|---|---|---|---|
| 1 | 实现线程安全的缓存 | 🟡 | ⬜ | |
| 2 | 实现线程安全队列 | 🟡 | ⬜ | |
| 3 | 自定义 string 实现 | ⚪ | ⬜ | |
| 4 | 避免使用有开销的 Magic Static | ⚪ | ⬜ | |
| 5 | socket 技术及 TCP、UDP | 🔴 | ⬜ | |

## 网络与内核旁路专题(占位 · 原笔记缺失,需外部补充)

| 主题 | 优先级 | 状态 | 备注 |
|---|---|---|---|
| 组播(Multicast)行情分发机制 | 🔴 | ⬜ | 原笔记 0 系统讲解;**已写入简历技能栏,需赶在投递/面试前补到可防御深度** |
| Kernel Bypass 原理(DPDK / 用户态网络栈) | 🔴 | ⬜ | 原笔记 0 提及;**已写入简历技能栏,需赶在投递/面试前补到可防御深度** |
| RDMA 基本概念 | 🟡 | ⬜ | 了解层面 |
| 交易所网关典型拓扑 | 🟡 | ⬜ | |
| TCP_NODELAY / 关 Nagle 实操验证 | 🟡 | ⬜ | 可深挖成对比实验 |

## 四、交易数据处理

| # | 主题 | 优先级 | 状态 | 强化笔记 |
|---|---|---|---|---|
| 1 | 任务一(数据流合并) | 🟡 | ⬜ | |
| 2 | 任务二(数据依赖问题) | 🟡 | ⬜ | |
| 3 | 任务三(csv↔binary) | ⚪ | ⬜ | |
| 4 | 任务四(增量计算与提前计算) | 🟡 | ⬜ | |
| 5 | 任务五(高频数据问题处理) | 🟡 | ⬜ | |
| 6 | 任务六(报单时序竞争分析) | 🔴 | ⬜ | 系统设计面试高频 |
| 7 | 任务七(服务访问日志分析) | ⚪ | ⬜ | |
| 8 | 任务八(期货逐日盯市处理) | ⚪ | ⬜ | |

## 五、业务逻辑设计-系统

| # | 主题 | 优先级 | 状态 | 强化笔记 |
|---|---|---|---|---|
| 1 | 订单管理系统 | 🔴 | ⬜ | |
| 2 | 订单簿设计 | 🔴 | ⬜ | |
| 3 | 消息传输协议引擎(FIX) | 🔴 | ⬜ | |
| 4 | 消息中间件(zmq) | 🟡 | ⬜ | |
| 5 | 交易系统的 OMS、EMS、PMS、RMS | 🔴 | ⬜ | |
| 6 | 订单簿历史查询 | ⚪ | ⬜ | |
| 7 | maker-only 撮合器 | 🟡 | ⬜ | |
| 8 | k 线生成器 | ⚪ | ⬜ | |
| 9 | 求解器 | ⚪ | ⬜ | |
| 10 | 智能订单路由(SOR) | 🟡 | ⬜ | |
| 11 | 自成交保护机制 | 🟡 | ⬜ | |
| 12 | 限流器 | 🟡 | ⬜ | |
| 13 | Lezer/Tree-sitter | ⚪ | ⬜ | 与 HFT 关联弱,可跳过 |
| 14 | 分布式交易系统共识模型 | 🟡 | ⬜ | 覆盖浅,见分布式占位 |
| 15 | 交易所协议分层与序列一致性 | 🔴 | ⬜ | |
| 16 | 预序列化订单 | 🟡 | ⬜ | |
| 17 | 自适应负载丢弃器(CoDEL) | ⚪ | ⬜ | |

## 分布式系统深化专题(占位 · 原笔记覆盖浅)

| 主题 | 优先级 | 状态 | 备注 |
|---|---|---|---|
| Raft 共识算法完整流程 | 🟡 | ⬜ | leader 选举 / 日志复制 |
| Paxos 基本思想 | ⚪ | ⬜ | 了解即可 |
| CAP 权衡在交易系统里的取舍 | 🟡 | ⬜ | 行情 vs 订单一致性 |

## 六、业务逻辑设计-计算(整章 ⚪ 泛读)

| # | 主题 | 状态 |
|---|---|---|
| 1–17 | 对数收益率 / 波动率状态 / 指标平稳性 / 信息熵 / 正则化模型 / 差分进化 / Avellaneda-Stoikov 做市 / 定点数 等 | ⬜ 泛读 |

## 七、数字货币

| # | 主题 | 优先级 | 状态 | 强化笔记 |
|---|---|---|---|---|
| 1 | uniswap-v2 | 🟡 | ⬜ | |
| 2 | uniswap-v3 | ⚪ | ⬜ | |
| 3 | CEX 订单簿方案 | 🔴 | ⬜ | 直接对应目标公司场景 |
| 4 | 三角套利 | 🟡 | ⬜ | |
| 5 | quant trading workflow | 🟡 | ⬜ | |
| 6 | Binance U 本位合约多因子 | ⚪ | ⬜ | 偏策略 |
| 7 | Gemini 标的 BBO 监控 | 🟡 | ⬜ | |
| 8 | polymarket 市场机器人 | ⚪ | ⬜ | |
| 9 | crypto 交易系统的监控 | 🟡 | ⬜ | |

## 面试落地项目清单(占位 · 知识点→实战产出)

| 项目 | 状态 | 备注 |
|---|---|---|
| 简化撮合引擎 + 订单簿数据结构(红黑树 vs 数组)延迟对比 | ⬜ | 出具体数字 |
| 无锁 SPSC 队列 vs `std::queue+mutex` benchmark | ⬜ | |
| 组播行情接收 demo | ⬜ | 配合网络专题 |
| 报单时序竞争复盘文档(带真实延迟数据) | ⬜ | 配合主题四-6 |

---

## 变更记录

| 日期 | 事项 |
|---|---|
| 2026-09-03 | 建表;开始 一/01 第 1 轮 |
| 2026-09-07 | 一/01 三轮讲完(绑核隔离 / 超线程+锁频 / NUMA);教学转叙述式;开始 一/02 |
| 2026-09-09 | 一/02 两轮讲完(调度策略 / 优先级反转+PI+PP+PREEMPT_RT);一/03 单轮(中断绑定实操)。§2 检验 Q3/Q4 偏弱,标记回炉 |
| 2026-09-09 | 一/01 叙述版三轮合一 + 拓扑 SVG;派生博客草稿(Systems,不上首页,未 push);index.html 加 `homepage:false` 过滤 |
| 2026-09-09 | 博客改英文 `cpu-affinity-core-isolation-numa.md`(删 `-cn` 草稿);拓扑 SVG 文字转英文;新增 §1 迁移代价概念插图(石墨铅笔风,提示词见文件注释);发现并遵循 `.trellis/spec/guides/illustration-style.md` |
| 2026-09-09 | 迁移代价插图(GPT 出图)入库;`89bf4bd` push main,CI 部署成功,首篇 Systems 博客上线;首页过滤已线上验证 |
| 2026-09-09 | §3 重讲并首次落文件 `03-中断绑定与核心隔离.md`(上一版丢在压缩的对话里);检验 3 题已回并批注:Q1 满分,Q2 缺命令序列,Q3 缺 managed IRQ |
| 2026-09-10 | §5 轮 1 首讲(4 根源 + STLF + TSO)落文件 |
| 2026-09-10 | §5 轮 1 **推倒重讲** —— 本人反馈"名词堆、无法理解";重写成单概念"小本子"(store buffer)故事,术语后置;§5 从 4 轮改 5 轮;检验换成"用自己的话讲" |
| 2026-09-10 | §5 轮 1 检验回过(Q1 回炉:机制三句要能复述)；轮 2 讲完(收件箱/四种乱序/x86 禁三放一/编译器),检验待回;STLF 挪轮 5 |
| 2026-09-11 | §5 轮 2 检验回过(Q1 满分 · Q2 轻回炉:编译器的锅 + 与内存模型无关 + 修法 · Q3 两理由对,补 x86 真错场景);轮 3 讲完(release/acquire 配对 = 发布订阅、六枚举地图、最弱够用三步、x86 白嫖 vs ARM ldar/stlr、MFENCE/SFENCE/LFENCE、volatile≠同步),检验待回 |
| 2026-09-11 | §5 轮 3 检验回过 —— Q1 回炉(release/acquire 机制对,"为什么必须成对"未答,已补 broken-pairing 例子)、Q2 轻回炉(两理由种子对但未展开,漏了"机器码相同"这个对的前提)、Q3 轻回炉(结论对、happens-before 链条未展开;顺带讲清 `std::atomic<bool>` 的 memory_order 是挂在每次操作调用上,不是挂在类型/变量上)。轮 4(伪共享/CAT)待讲 |
| 2026-09-14 | §5 轮 4 讲完 —— 缓存行是搬运最小单位(64B)、伪共享 ping-pong 重放(两个无关变量抢同一缓存行独占权)、`alignas(64)`/`std::hardware_destructive_interference_size` 修法、真共享对比(`atomic`/`thread_local`)、干跑双副本反用预热(源笔记特色技巧,标注适用边界)、Intel CAT/pqos 划 L3(容量争用,跟伪共享的协议争用区分开)。检验 Q1/Q2/Q3 已出,待回 |
| 2026-09-14 | §5 轮 4 检验回过 —— Q1 回炉(把独占权来回抢讲成了轮2的"通知+重读",漏了轮4核心机制)、Q2 本人提出好问题"为什么热L3没意义"而非作答,已展开讲清 L1/L2 私有 vs L3 共享被吵闹邻居抢占、CAT 才是 L3 层真正解法、Q3 对(alignas=协议层独占权 vs CAT=容量层吵闹邻居)。轮 5(流水线ILP,§5 最后一轮)待讲 |
| 2026-09-14 | §5 轮2/轮4边界三连追问,本人主动带外部材料核对("写-读乒乓"vs"写-写乒乓")—— 确认轮4写写乒乓机制对;指出材料对轮2的合理扩展(A/B都在循环里,收件箱机制也会反复发生、不只一次性过期读)已补入笔记,含批量通知+影子副本(LMAX Disruptor 式本地缓存游标)修法;纠偏"受害者只是读者"(写者等invalidate确认也有代价);"Write-Read Ping-Pong"英文标签未确认是标准术语,已标注保留 |
| 2026-09-14 | 纠正上一条留下的表述模糊:明确"轮2收件箱=正确性问题(内存序,轮1-3管)"vs"轮4伪共享=性能问题(跟读没读错无关,轮4管)"是两个独立维度、同一套硬件零件,不是谁包含谁;补证据(counter例子零atomic也伪共享 vs SPSC tail 需同时管两件事)落笔记最前面 |
| 2026-09-14 | §5 轮5 补充:本人带外部材料(多累加器循环展开、latency×throughput 累加器数公式)核对+追问"8个能省多少"——公式验证为标准手法(Agner Fog 式推导),算出 min(端口,K÷延迟) 表格(1/4/8/16 累加器→0.25/1.0/2.0/2.0 FMA/周期,4→8 理论再翻倍);补实测会打折的诚实提醒(读数据端口/内存带宽可能先于FMA端口封顶,compute-bound vs memory-bound)。已落笔记"三优先"节后 |
| 2026-09-14 | §5 轮5 检验回过 —— Q1 轻回炉(写法A关键路径不是4乘+4加,是1乘+4加,只有第一个乘法在链上;写法B的1乘+2层加法二叉树对且推导讲究)、Q2 非考点、本人追问"false dependency常见吗/AI能识别吗"已答(源码不可见 vs 依赖链源码可见的区别;AI能知识匹配、不能替代perf实测)、Q3 轻回炉(STLF关系和规则对,补"转发只认单条记录不做拼接"机制)。**§5(内存模型/缓存/流水线)五轮全部讲完检验完,是全篇最大最难一章;下一步进入 三(socket 编程)** |
| 2026-09-14 | §5 轮 5 讲完 —— link链式依赖vs独立计算排布对比(同运算量快一倍)、流水线5段+数据/控制两类停顿(接回轮2推测执行)、三优先(拆依赖链/数据入寄存器/指令类型均衡)、false dependency(popcnt目标寄存器伪输入,xor清零破)、register spill(16 GPR,三优先第一条用过头反噬的反讽)、STLF 接回轮1小本子(写读宽度对齐要求)。加"§5 五轮收口"段落把正确性轴(轮1-3)/性能轴(轮4-5)、同一套硬件家底这条线钉进笔记。§5 全五轮讲完,检验 Q1/Q2/Q3 已出待回,是全篇最后一轮检验 |
| 2026-09-15 | §5 检验全部批完,§5 收尾。误判"讲完§5=讲完阶段一",直接跳去三(socket)开始备课——用户当场发现叫停("为啥突然调这么远到socket了"),未产生实际损失。拉回一,确立"🔴完整轮 / 🟡⚪轻讲不出题"深度分级规则(写入 README + tutoring 记忆);下一步改为按编号顺序回到 #4 |
| 2026-09-15 | 一/#4(系统静默配置步骤总结)轻讲版讲完(不出检验题)——四类启动参数(时钟中断隔离/频率与电源状态锁定/内存与IO局部性/RDTSC计时校准)+ process_helper 代码(纯 §1+§2+§3 的生产化封装,无新概念)。落文件 [04 笔记](01-低延迟系统开发基础/04-系统静默配置步骤总结.md)。下一步:#6 内存池(🔴,完整轮) |
| 2026-09-16 | 一/#6 内存池 轮2(内存调优 mlockall/mmap坑/M_TRIM_THRESHOLD/M_ARENA_MAX + TLB shootdown 机制/isolcpus挡不住/连回§4 THP)检验回过——Q2/Q3 扎实(Q3 主动点出 `mm_cpumask` 内核结构,比原讲解更precise);Q1 轻回炉:munmap 撤销 mlockall 锁的因果("映射被连根拔起、锁是映射属性"这步没点破)+ M_TRIM_THRESHOLD 和 M_ARENA_MAX 职责混淆。落文件 [06 笔记·附二](01-低延迟系统开发基础/06-内存池.md)。下一步:轮3 pinned_arena + LowLatencyAllocator |
| 2026-09-16 | 一/#6 内存池 轮3(pinned_arena + 自定义 STL 分配器 LowLatencyAllocator)讲完+检验回过——Q1 扎实,比讲解本身更深(抓到"没有per-分配元数据"这个根子);Q2 两次作答:第一次逐字复述讲解内容被打回重讲,第二次机制正确但漏了"不同机制、同一模式(不确定操作提前挪到启动阶段做完)"这层,已点破。Q3 是笔者自己读代码发现的〔补〕内容,非源笔记本身——`MemoryBlock::data()` 的 `this+1` 跟 `arena_mem_.blocks[i]`/`data_start` 的内存布局假设对不上,真实会写坏下一块的 header,demo 数据量太小没暴露;这道题没走成一次性作答,是经"这是 demo 问题吗/这知识点有什么用/读哪里代码发现的/真有问题吗/要不要改"几轮追问才确立理解,已给出修法(预先存 `data_ptr`,不再临时靠 `this+1` 猜)。过程中两次教学方式反馈已记入 tutoring 记忆:轮 3 第一版讲解"堆术语"过密被打回、按"直觉先行+严谨版收尾但一次讲完整轮"的格式重讲;后段追问时被指出回答"不知所云"、要求是非题先给结论再展开。§6 只剩 `std::pmr` 一轮。落文件 [06 笔记·附三](01-低延迟系统开发基础/06-内存池.md)。 |
| 2026-09-17 | 一/#6 内存池 轮4(`std::pmr` 多态内存分配器)讲完+检验回过,**§6 内存池四轮全部完成**——轮4 讲了类型不兼容问题(分配器是模板参数)、`memory_resource`/`polymorphic_allocator` 把静态多态换成动态多态、内置几种 resource 与前三轮的对应关系、以及源笔记点名的生命周期悬空坑(`create_vec()` 返回 pmr::vector 但底层 resource 是局部变量,函数结束即悬空)。检验:Q1 过(前半句贴近原句,后半句"运行时多态转发"是自己的收束);Q2 两次作答,第一次判定复述打回,第二次自己的话过,轻回炉"析构≠deallocate"+"reset"方法名未经核实;Q3 全程自己的话,轻回炉"悬空指针的持有方向"+"临时对象"术语误用。期间 WebSearch 工具报错,无法核实 `monotonic_buffer_resource::release()` 细节,已如实告知未核实、不当确定结论记录。教学格式本轮也经历三次修正才定型(讲太密→碎片化太细→"直觉/严谨版"两遍式仍不对),最终定为"一轮完整讲完,但每个概念一遍到底、术语随讲随定义",已写入 tutoring 记忆。落文件 [06 笔记·附四](01-低延迟系统开发基础/06-内存池.md)。下一步:按编号顺序 #7/#8(🟡,轻讲) |
| 2026-09-15 | §5 博客写完并上线(commit `a479036`);发现 GitHub Pages Source 一直是 legacy 且 `docs/` 早于两篇文章就没再提交,两篇文章线上全 404;改 Source 为 GitHub Actions + `docs/` 移出 git 追踪(commit `618ea94`),两篇文章和 `/categories/systems/` 复核均 200 且图片/SVG 正常渲染 |
| 2026-09-17 | 一/#7(大页内存)+ #8(内联及内联汇编)轻讲版讲完(不出检验题)。#7:TLB/页表层级原理、四种大页获取方式对比(mmap 显式大页是 HFT 实际用的,THP 要禁用且与 #4 关 THP 呼应)、`HugePageAllocator` 纯静态工具类(只换池子的存储来源,free list/bump pointer 逻辑不变);并解答追问"`HugePageAllocator` 用 `mmap` 是否违反轮 2'避免 mmap'的规则"——不矛盾,轮 2 防的是分配器在热路径不可预测时机背着你调 `mmap`/`munmap`,这里 `allocate`/`deallocate` 只在启动/销毁阶段各调一次,不在热路径上。#8:`inline` 的链接期(ODR)含义与编译器优化建议是两个独立含义共用一个关键字、`always_inline`/`flatten`、错误处理内联的 i-cache 代价、内联汇编 GNU 风格(输出/输入/clobber 四段结构)、VEX 混用 penalty(约70周期)、手写汇编技巧表(挑出"xor 清零打破伪依赖"接回 §5 轮5 `popcnt` false dependency 的修法)。落文件 [07 笔记](01-低延迟系统开发基础/07-大页内存.md)、[08 笔记](01-低延迟系统开发基础/08-内联及内联汇编.md)。下一步:#9 lock-free queue(🔴)。 |
| 2026-09-18 | 一/#9 lock-free queue 及 micro-batching(🔴)轮 1 讲完检验完——`LFQueue<T>` SPSC 环形队列:2 的幂容量+mask 替代取模(接回 #8 除法优化思路)、生产者/消费者两步接口(先拿槽位指针直接写、再单独发布索引,省一次拷贝)、memory_order 选择接回 §5(自己写的 relaxed、对方写的 acquire、release/acquire 配对)。检验:Q1 轻回炉(2 的幂↔mask全1↔&等价于%这步机制没落地)、Q2 部分对(acquire 侧"先清空 invalidate queue"是自己综合的好答案;relaxed 侧把 STLF 硬件细节和"内存模型单线程程序序保证"这两层因果搞混,轻回炉)、Q3 判定为出题出弱(讲解里已讲完后果,不该再要求复述,撤销此类问法)、Q4 讨论出"num_elements_ 独占权代价 vs 两个原子量现算 size()"的完整机制(§5 轮4"独占权来回打"的重现),并〔补〕在源文件注释掉的替代版 size() 里发现一个采样顺序坑(先读 write 后读 read 会下溢,应反过来)。本轮顺手抓到一个真实 bug——`tryGetNextToWriteTo()`/`is_full()` 里 `&` 和 `==` 混写忘加括号,C++ 里 `&` 优先级低于 `==`,导致"队列满"判断恒假,生产者会覆盖未读数据。本轮也确立一条格式规则:讲解要用 markdown 结构(标题/列表/独立代码块),不能挤成大段连续文字(已写入 tutoring 记忆)。落文件 [09 笔记·附一](01-低延迟系统开发基础/09-lock-free-queue及micro-batching.md)。下一步:轮 2(SPSC 缓存友好优化 + double-mmap 环形缓冲区)。 |
| 2026-09-18 | 一/#9 轮 2 讲完检验完——本地缓存对方游标(`head_cached_`/`tail_cached_`,只在跨界时才付一次 acquire 代价),接回 §5 轮2/4 边界追问里预告过的"影子副本"(LMAX Disruptor 手法首次落进真实队列类);double-mmap 环形缓冲区跨尾写入优化(`memfd_create`+`ftruncate`+两次 `MAP_FIXED` 映射同一 fd,让跨尾写变成一次连续 `memcpy`),接回 #7 的 TLB 压力代价,是"构造期一次性 mmap、零热路径系统调用"模式第三次重现(§6轮2→#7→这里)。检验:Q1 两分支拆解正确且独立作答,轻回炉"为什么恰好该在追上缓存值那一刻重新读"这层单向性论证;Q2 结论对,但**题目本身设计有问题**——答案已在正文讲完,等于要求复述,跟轮1 Q3 是同一类缺陷,虽然说过不会再犯还是再犯了,已如实记录并加进 tutoring 记忆(以后出题前自查是否已经在正文给过答案);Q3 对,独立压缩表达抓住机制,顺手连上 #7 的 SysV 共享内存。落文件 [09 笔记·附二](01-低延迟系统开发基础/09-lock-free-queue及micro-batching.md)。下一步:轮 3(Logger 基于 LFQueue + MicroBatchProcessor 动态微批处理)。 |
| 2026-09-20 | 一/#9 轮 3 已讲(Logger 基于 LFQueue:`LogElement` 定长 tagged union、热路径两步 push + 后台线程批量 flush + 析构先排空再停线程;MicroBatchProcessor:按积压量三档批大小,`message_queue_size` 与轮 1 `num_elements_` 是同一模式;〔补〕真 bug:`std::function<void(const std::vector<Message>&>)` 尖括号/圆括号顺序反了、`LOW/HIGH_BACKLOG_TIMEOUT_US` 声明了没用),检验 Q1–Q3 待回,**尚未归档**。期间用户带外部材料(一段"SPSC=wait-free、后台线程崩了生产者也不受影响"的说法 + 两个知乎链接)问 wait-free 有没有覆盖——有:源笔记一/#30(🔴,按序在后)就是知乎文章逐行讲解的同一个 sticky counter 例子(搜索页没读到,文章用内置浏览器读到全文)。缺口(四档进度保证梯度、SPSC 是否 wait-free 及前提、lock-free 对照版)中现在要用的部分提前落 [09 笔记〔补〕](01-低延迟系统开发基础/09-lock-free-queue及micro-batching.md),#30 备课清单记在 #30 行;#30 仍按编号顺序讲,未提前。核对结论:SPSC 是 wait-free,但仅限"满了就失败返回"的接口(`getNextToWriteTo()` 自旋版是阻塞式,Logger 的 `pushValue` 用的正是它);"后台线程崩了生产者也不受影响"说过头,队列满了就会丢或阻塞,Logger 靠 8M×264B≈2.06 GiB 大缓冲缓解;"用无锁队列是为可预测而不是更快"这半句对。顺带修正轮 1 笔记一处不严谨:优先级 bug 让表达式"几乎恒为 0"而非"恒为 0"(读索引恰走到 `mask_` 且 `current_write+1` 为奇数时会误报一次满)。 |
| 2026-09-20 | 用户说可以结合博客补充笔记 → 写了一/#30 **预读稿**([30 笔记](01-低延迟系统开发基础/30-wait-free编程.md);未在对话里讲、无检验,#30 仍按编号顺序讲完整一轮)。内容:sticky counter 的 lock-free 对照版、wait-free 版"窗口"/helping 逐步走查、取舍与 ARM 无 LSE 的硬件依赖。写之前先用 Python 把三个操作的所有线程交错穷举([脚本](01-低延迟系统开发基础/30-sticky-counter-model-check.py);≤4 线程、顺序一致模型,不是证明):源笔记代码在加/减/零判断上 0 违规;三处设计(读者帮忙钉零、`decrement` 识别 `helped`、`exchange` 认领)各去掉一处都立刻出反例(零不稳定 / 泄漏 / 重复销毁),据此**纠正了知乎文章对 `exchange` 的解释**(它是"认领权只发一张"的 test-and-clear,不是核实标记归属);另发现 `read()` 返回的非零数值只是近似值(有最小反例,已附在笔记里)。同页推荐栏的两篇文章只记了标题、未读。 |
| 2026-09-20 | 用户贴了一段外部分析("`log()` 逐字符 push 是致命性能缺陷,工业界改法:传字符串指针 / 段压入 / 编译期解析格式串")问什么意思,并说"要不你直接改"。已讲清并核对:文中数字量级对(数得 30~31 次 push,每次 264 B 而非 256 B,约 7.7 KiB/条),"打爆队列"夸张,真问题是热路径成本(30 次 `lock` 指令,轮 1 Q4 放大 30 倍)、缓存污染、有效缓冲缩水(8M 槽只够约 28 万条日志);占位符在这个 Logger 里是单个 `%`,不是 printf 的 `%d`。NanoLog/Quill 两个库的 README 用内置浏览器读了(WebFetch 那次因总结模型不可用报错),文中的 "Quilby" 应是 Quill 的笔误。**改法落在 09 精读笔记,源笔记文件没动**(改了所有 `:行号` 锚点会漂移):[09 笔记·轮 3](01-低延迟系统开发基础/09-lock-free-queue及micro-batching.md) 里写了改进版 `log()`——段压入,只改 `log()`、消费者不动,示例 30 次 push → 5 次;传指针/编译期解析要改协议,列为方向。这段 C++ 没有编译过(机器上没有编译器,WSL 也没装发行版),用 Python 把原版/改进版逻辑各写一份([脚本](01-低延迟系统开发基础/09-log-literal-run-check.py))对比 30 万组随机格式串 + 边界情形,输出与报错类型一致。同时把已讲的轮 3 整体归档进 09 笔记(此前只在对话里),检验题 Q3 换成新的("传指针的办法能不能用于 `std::string` 参数"),Q1/Q2 不变,均待回。 |
| 2026-09-20 | 一/#9 轮 3 检验回过,**#9 lock-free queue 三轮全部完成**。Q1:方向对(接回 §5 轮 4 抢独占权,并主动用上附一〔补〕的 `size()` 先读 tail 后读 head),补一个答案默认的前提——泛型 `MicroBatchProcessor` 对 `QueueType` 只用 `enqueue`/`dequeue`,看不到 head/tail,想省计数器得先把 O(1) 近似 `size()` 写进接口要求;"物理上严格等于"收紧为"近似快照"。批改中发现并**更正自己的一处错**:附一〔补〕的 `size()` 反例把调用者写成"消费者自己",不成立(自己的游标在自己执行 `size()` 期间不会动)——用小的交错穷举模型核对([脚本](01-低延迟系统开发基础/09-size-read-order-check.py)):先读 tail 后读 head 对任何调用者都不出负数;先读 head 后读 tail 只有第三方线程调用会出负数,生产者/消费者自己调用两种读序都安全。另推演出独立计数器不是严格值(`enqueue` 与 `fetch_add` 之间不原子,`size_t` 可瞬间下溢),未实测。Q2:答对。Q3:答"不能"(对)并自述"没懂"——这题用到的"生命周期"这层我没讲过就问了,是出题的问题;已补讲:判据是生命周期+不可变,不是"是不是字面量";源码 :349-357 本来就把 `std::string` 拷字节进槽(超 255 字符静默截断);讲 #10 时接回。**用户在本轮批改中给的出题反馈**:①"是不是复述"我追得过头了——定位到关键句本身也能说明理解,再这样下去会不敢对关键知识点出题;想避免纯复述,应该挑需要理解的点追问,而不是砍题;②不要直接问没讲过的知识点(Q1 的接口提示、Q3 都属此类),那不是在考理解。出题原则据此改写(见 tutoring 记忆),此前"出题前自查答案是否已在正文给过、给过就砍"的规则作废。落文件 [09 笔记·附三](01-低延迟系统开发基础/09-lock-free-queue及micro-batching.md)。下一步:#10 spmc 共享内存无锁队列(🔴);#9 已完整讲完,博客 #4 是否现在写,待用户定。 |
| 2026-09-20 | #9 写成博客(系列 #4)——草稿 [lock-free-queue-logger-micro-batching.md](../../../../jasper-aa64.github.io/content/posts/lock-free-queue-logger-micro-batching.md)(博客仓库,**未提交、未发布**)。结构:环形队列(掩码/两步交接/memory_order)→ 减法代替计数器(共享计数器是真共享的独占权乒乓;`size()` 先读落后的那个)→ 缓存对方游标(只会往安全方向错)→ 双映射环形缓冲(镜像地址而不是数据,只对字节流/批量拷贝有用)→ Logger(定长 tagged union,264 B)→ 一条日志 30 个槽(内嵌 SVG;段压入 / 传指针 / 编译期解析,指针只对活得比读者久且不变的东西成立)→ 进度保证梯度(SPSC 只有失败即返回的接口才是 wait-free;有限步≠有限时间;有界队列必须选丢或阻塞)→ 微批处理(积压量选档位、不等攒满;积压量来自队列而不是计数器)→ Recap。按此前定下的风格写:frontmatter 短标题 + 正文长 H1、纯知识点总结、不写"发现源码 bug"的叙事、不加来源署名(优先级 bug 只作为一条通用的运算符优先级陷阱放进第 1 节)。SVG 已在浏览器里核对(39 个 rect / 9 个 text,0 个 stray `<p>`,亮/暗色都正常)。 |
| 2026-09-20 | #9 博客(系列 #4)**已提交并推送**(commit `a443bd2`,只含新文章和新配图;`hot-path-memory-allocators.md` 里原有的未提交改动没动),CI "Build and Deploy" 31 s 成功,live DOM 核对通过(数值见上面"博客发布"一节)。应用户"缺的你直接装好":装了 **Hugo 0.160.1 extended**(与 CI 同版本,sha256 与 release 校验和一致)和 **GCC 16.2.0**(WinLibs,MinGW-w64 UCRT,posix 线程;zip sha256 与 GitHub digest 一致),都是免安装 zip,放在 `C:\Users\liangjunming\tools\`,没有改 PATH。发布前把文章里的 C++ 代码块原样抽出来编译运行:10 个可编译块 0 警告;SPSC 队列(基础版 + 缓存游标版)在容量 2/8/1024/65536 下两线程各传 300 万~2000 万个元素,顺序与内容全对;逐字符版与文本段版 `log()` 在 60,008 组随机格式串(含 >255 字符的字面量和字符串实参)上输出与期望完全一致,示例行 30 次 push(7,920 B)对 5 次 push(1,320 B),`sizeof(LogElement)==264` 通过 static_assert;微批处理三档与 concept 行为正确。`size()` 读序:观察线程约 16.7 亿次采样中,先读 tail 再读 head 从未出现负数、但有 251 次高估超过容量;先读 head 再读 tail 出现约 1.1 万次负数——据此在文章第 2 节补了一句"先读落后的计数器得到的是高估,不会为负但可能超过容量"。限制:这台机器的 MinGW GCC 没有 TSan/UBSan 运行库(`-ltsan`/`-lubsan` 缺失),TSan 需要 Linux(WSL2 要开 Windows 功能并重启,由用户决定);`mmap`/`memfd_create` 只能用桩函数做语法检查、没法运行。 |
| 2026-09-20 | 一/#10 spmc 共享内存无锁队列(🔴)**轮 1 已讲并落笔记**([10 笔记](01-低延迟系统开发基础/10-spmc共享内存无锁队列应用.md)),检验 Q1–Q3 待回。内容:场景(一个生产者、多个下游、每个都要每一条 = 广播,不是 #9 的分活;生产者不能等任何读者)→ 数据结构(每块自带序号戳 `idx`,`write_idx` 只有生产者写,`CNT` 为 2 的幂,32 位序号会回绕)→ 生产者(原地填数据、最后 release 盖戳,从不读读者状态)→ 读者(`Reader` 在读者自己内存里、私有游标 `next_idx`;`int(new_idx - next_idx)`:<0 无新消息 / =0 正好 / >0 被套圈跳过)→ 内存序(戳即发布信号,接回 #9 与 §5)→ 不保证什么(默默丢消息;撕裂读)。**验证**:把源笔记里的 `SPMCQueue` 与 Lev2 结构体原样用 g++ 16.2 编译运行([行为检查](01-低延迟系统开发基础/10-spmc-behavior-check.cpp)、[撕裂读检查](01-低延迟系统开发基础/10-spmc-torn-read-check.cpp)):`sizeof(Lev2Trans)`=88 B、`Lev2Order`=80 B、`Block`=128 B、一个队列 67,108,992 B ≈ 64 MiB;空队列读得 `nullptr`;历史不回放;CNT=8 写 20 条后读者拿到 17/18/19/20,1~16 丢失且无提示;`readLast` 返回最新一条;32 位回绕点有符号差值正确、直接比较判反。撕裂读(CNT=4、生产者全速、每组 3 秒,故意最恶劣):源码原样约 5.5% 的被接受读取撕裂(3,269,273 / 59,869,704);"读完再重读 idx"防不住(仍有 2,084,572 / 43,942,557,约 4.7%),因为生产者先写数据、最后才改戳;生产者改成先作废戳(idx=0)、再写数据、再盖戳,读者读完重检 → 38,607,078 次接受、0 次撕裂。这一点是自己测出来的,源笔记没有提。出题按用户 2026-09-20 的新反馈:只问本轮讲过的点、允许复述式作答、深度靠追问——Q1 对比 #9 的"为什么不读读者状态/换来什么付出什么"、Q2 三种情形 + 差值 +5 时的后果、Q3 "重读 idx 没变能不能断定数据完整"。下一步:等作答批改 → 附一 → 轮 2(共享内存落地 + Level2 应用)。 |
| 2026-09-24 | 博客 #4 收尾核对 + 提出 MESI 补课。①插图映射错误已在 origin/main 修好(`d8a0dff` 2026-09-21 换成三行对齐版,`7eed74a` 转 JPG);本机工作区干净、与 origin/main 一致,无待推内容;线上 JPG 哈希 = 仓库文件;残留一个小瑕疵(弯钩箭头落在第二行右端)。②用户指出 MESI"频繁提到但没讲过":核对属实——源笔记只有引用(05:35、05:199-203 两个外链、09:8、24:387),精读 #5 轮 2/4 讲了机制但 :450 明写"不用背",#9 :27 只点了名。决定:开补课 5补(🔴),笔记 `05-补-MESI缓存一致性状态机.md`,排在 #10 收尾之后、#11 之前;用户贴的外部 5 场景总结核出 5 处问题,不原样收录(见 5补 行)。#10 轮 1 检验 Q1–Q3 仍待回。 |
| 2026-09-24 | 一/#10 轮 1 检验回过 + 轮 2 讲完。**批改**([10 笔记·附一](01-低延迟系统开发基础/10-spmc共享内存无锁队列应用.md)):Q1 方向对、"读者增加不影响生产者速度"过强——实测(Ryzen 5 5600GT/Windows,读者忙轮询)生产者每条 2.8 ns(0 读者,512 KiB 环)→ 18.5(1 个)→ 28.0(5 个);64 MiB 环 12.6 → 26.0(读者读载荷 38.7);放慢到每 2 µs 一条时 write() 中位数不变(10 ns)、p99.9 从 20 涨到 70 ns。Q2 对,补:差值必是 CNT 的整数倍("+5"只是举例)、diff>0 的信息在 read() 里现成但源码不交出来、被套圈后落到哪块就从哪块往后读、不回头。Q3 对,追问放进轮 2。**轮 2**:shmmap 四步 + 靠零初始化当空队列;两进程实测(Windows 命名映射):同一队列 3/3 收到,view 地址不同(0x17987cc0000 vs 0x1ae3be70000),同 exe 的字面量地址碰巧相同——所以元素必须自包含;libstdc++ 短 string 的 data 指针指向对象自己内部;三个坑:第一圈缺页(不预热 56.1 ns/条、p99 1.3 µs,预热 9.0 ns;每 32 条一页、约 1.5 µs/次)、慢读者被套圈(余量 = 容量 ÷ 速率差)、重启续读(getReader 丢 6~9,存 next_idx 不丢;落后超一圈跳过 24 条且随后一条不读;队列重建后老读者卡死)。**更正**:09 附三〔接回 #10〕原写"连字面量的地址通常也不能用",实测同一个 exe 起的两个进程里相同,改为"不能依赖"。检验 4 题待回(含轮 1 Q3 的追问)。下一步:等作答批改 → 附二 → MESI 补课 → #11。 |
| 2026-09-24 | 用户两条节奏要求:①批改完就停,等"继续"再讲下一个(我上一条把轮 1 批改和轮 2 讲课放在同一条消息里了);②在学习,不要默认在本机跑实验、费时间(已写入记忆,并把 5补 行里的"配小实验"去掉)。用户带来外部 AI 对附一 Q1 数字的解释并追问"大环里写前是不是 I / I→M 与 S→M 谁贵":已答并落 [10 笔记·附一〔补〕](01-低延迟系统开发基础/10-spmc共享内存无锁队列应用.md)——大环写前是 I(对);两者无固定高低,看数据从哪层取 + 是否跨核确认;外部回答的"512 KiB 环是 L1 命中""S→M 较低""1 读者时读者核里还有旧的 S 副本"不采信;我自己"怎么读这组数"的解读也收窄到小环。10 轮 2 的 4 题仍待回。 |
| 2026-09-25 | **笔记迁入独立仓库,此后以仓库为准**(用户:"之后准备用仓库维护了,不用本地维护了";要的是一个新仓库)。仓库:`Jasper-aa64/trading-system-notes-study`(私有,`main`),仓库根 = 原来的 `01-博客与资料/trading-system-notes-精读/` 目录,相对链接一个没改。(我最初误把它推成 HFT-wf 仓库里的分支 `study/trading-system-notes`(`8596b3df`),随后迁到本仓库,那个分支作废。)新增:[CLAUDE.md](CLAUDE.md)(把本机记忆里的教学规则搬进仓库)、`tools/bootstrap_source.py` + `tools/_split_notes.py`(重建 `:line` 锚点指向的源笔记,生成在本仓库目录的同级)、[docs/illustration-style.md](docs/illustration-style.md)(博客配图风格规范,逐字拷自 HFT-wf)、`.gitignore` / `.gitattributes`。**发现并处理**:笔记里全部锚点指向的 `chinese/` **不在上游里**——是本地用 `_split_notes.py` 从上游单文件 `trading-system-notes-Chinese.md` 拆出来的;其中 §1 `01-cpu亲和性及numa架构.md` 与 §4 `04-系统静默配置步骤总结.md` 两份在本地又被格式化过(补空行、对齐表格),从固定版本重建出来的行号最多差 3 行。已把这两份笔记里 22 条 `:line` 锚点按"同一行文本"对准到可重建的版本(除 `:行号` 外逐字未改;其余 137 条本来就一致;170 条链接全部解析);上游没有许可证,所以没把它的文本提交进仓库。原来本机那份未跟踪的旧目录作废,不要再改。10 轮 2 的 4 题仍待回。 |
